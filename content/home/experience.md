---
# Moved to its own standalone page at content/experience/ (see config/_default/menus.toml).
# This stub overrides the theme's own content/home/experience.md fallback, which would
# otherwise leak through and render on the homepage since our site no longer has one here.
widget: experience
active: false
headless: true
---
